
<div class="container mx-auto px-4 mt-8 max-w-xs py-16">
	<%= form_with(model: @chair) do |f| %>
		<h1>Editing <%= @chair.company %> <%= @chair.name %></h1>
		<%= f.label :name, class: label_classes %>
		<div class="mt-2">
			<%= f.text_field :name, class: text_field_classes, placeholder: "you@example.com" %>
		</div>
	<%end %>
</div>